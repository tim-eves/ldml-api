apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-sets
  namespace: wstech
spec:
  #storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
